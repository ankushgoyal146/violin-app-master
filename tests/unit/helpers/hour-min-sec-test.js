import { hourMinSec } from '../../../helpers/hour-min-sec';
import { module, test } from 'qunit';

module('Unit | Helper | hour min sec');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = hourMinSec(42);
  assert.ok(result);
});
